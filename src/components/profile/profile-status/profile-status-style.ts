import styled from "styled-components"

export const Container = styled.div`
  grid-area: main;
  .status {
    color: #fff;
    font-size: 24px;
    cursor: default;
  }
  .status-owner {
    color: #fff;
    font-size: 24px;
    cursor: pointer;
  }
`