import React from "react";
import {Container, Spinner} from "./preloader-style";

const Preloader = () => {
    return (
        <Container>
            <Spinner/>
        </Container>
    )
};

export default Preloader;